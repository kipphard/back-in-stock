/* Wieder verfügbar – Admin JS */
( function () {
	'use strict';

	// Platzhalter für künftige Admin-Interaktionen (z.B. CSV-Export-Feedback).
	document.addEventListener( 'DOMContentLoaded', function () {
		// Derzeit keine clientseitigen Admin-Aktionen erforderlich.
	} );
}() );
